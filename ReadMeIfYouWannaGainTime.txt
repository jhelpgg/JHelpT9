If you want avoid the very first initialization of T9, you can unmcopress the file "PrecomputedT9.zip" near the jar that contail JHelUtil (Or if not inside a jar, beside the bin directory where JHelpUtil is compiled.
Note only French T9 is actually done, for other language add them in resources. You will see create T9 map and word lists for a language is easy, but take time.
